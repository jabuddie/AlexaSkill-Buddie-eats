import { services } from 'ask-sdk-model';
import { PersistenceAdapter } from 'ask-sdk-core/lib/attributes/persistence/PersistenceAdapter';
import { ErrorMapper } from 'ask-sdk-core/lib/dispatcher/error/ErrorMapper';
import { HandlerAdapter } from 'ask-sdk-core/lib/dispatcher/request/handler/HandlerAdapter';
import { RequestMapper } from 'ask-sdk-core/lib/dispatcher/request/mapper/RequestMapper';
/**
 * An interfaces that represents the standard components needed to build {@link Skill}.
 */
export interface SkillConfiguration {
    requestMappers: RequestMapper[];
    handlerAdapters: HandlerAdapter[];
    errorMapper?: ErrorMapper;
    persistenceAdapter?: PersistenceAdapter;
    apiClient?: services.ApiClient;
    customUserAgent?: string;
    skillId?: string;
}
